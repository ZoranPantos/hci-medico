<Window x:Class="HciMedico.App.Views.LoginView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:HciMedico.App.Views"
        xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml"
        mc:Ignorable="d"
        Title="Medico Login" Height="250" Width="400"
        Background="{DynamicResource LoginBackgroundBrush}"
        WindowStartupLocation="CenterScreen">

    <Window.Resources>
        <LinearGradientBrush x:Key="LoginBackgroundBrush">
            <GradientStop Offset="0" Color="#90CAF9" />
            <GradientStop Offset="1" Color="#7E57C2" />
        </LinearGradientBrush>
    </Window.Resources>

    <StackPanel Margin="10">
        <ui:TextBox x:Name="Username" Margin="0,0,0,5"
                    PlaceholderText="Username" Background="White" />
        
        <ui:PasswordBox x:Name="Password" Margin="0,0,0,10"
                        PlaceholderText="Password" Background="White" />
        
        <ui:Button x:Name="Login" Content="Log in" Appearance="Primary" />
    </StackPanel>
</Window>

<!--Background="#C5CAE9"-->