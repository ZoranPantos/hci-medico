<Window x:Class="HciMedico.App.Views.ShellView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml"
        xmlns:local="clr-namespace:HciMedico.App.Views"
        mc:Ignorable="d"
        Title="Medico" Height="450" Width="800"
        Background="#E0F2F1">
    
    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <ui:TextBox x:Name="TestTextBlock" Background="White" />
        
        <ui:Button x:Name="TestButton" Grid.Row="1" Content="Test button"
                   Appearance="Primary" Margin="0,20,0,0"/>

        <ContentControl x:Name="ActiveItem" Grid.Row="2" Margin="0,20,0,0"/>
    </Grid>
</Window>
